# URL base da API REST do backend (repositorio separado).
# Enquanto o backend nao estiver disponivel, o front roda sobre dados mock (ADR-0001).
VITE_API_URL=http://localhost:3000

# Liga a camada de dados mock. true = nao chama a API real.
VITE_USE_MOCK=true
